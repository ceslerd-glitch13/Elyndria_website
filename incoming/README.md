# Incoming token artwork

Upload JPG, PNG, or WebP images into this folder. The automation converts valid artwork and removes the original after processing.

Use neutral filenames because repository history is public. Do not include enemy names or plot spoilers in filenames.
